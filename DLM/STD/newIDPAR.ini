// GetIdPar bestimmt, wie eine eingegebene Artikelnummer zu formatieren
// ist. Hier werden numerische Eingaben rechtsbÅndig ohne fÅhrende
// Nullen formatiert.
GetIdPar {|x|if(val(x)==0,x,str(val(x),LenIdPar(),0))}
NextIdPar {||str(val(DbfNextId(oPar(),"",LenIdPar())),LenIdPar(),0)}

