// GetIdArt bestimmt, wie eine eingegebene Artikelnummer zu formatieren
// ist. Hier werden numerische Eingaben rechtsbÅndig ohne fÅhrende
// Nullen formatiert.
GetIdArt {|x|if(val(x)==0,x,str(val(x),LenIdArt(),0))}
NextIdArt {||str(val(DbfNextId(oArt()," ",LenIdArt())),LenIdArt(),0)}

